PORT=8000

# Mail Service
SMTP_USER=dev.creation.dream.stu@gmail.com
SMTP_PASS=syvo hstq kade hjah

# SQL Server connection (SSMS or Azure SQL)
DB_TYPE=sql                             # sql = SQL Server, azure = Azure SQL
DB_SERVER=.\SQLEXPRESS                  # .\SQLEXPRESS nếu sử dụng SSMS hoặc localhost nếu sử dụng Azure SQL
DB_NAME=SOA_GK
DB_PORT=1433
DB_USER=sa                              
DB_PASSWORD=yourStrong(!)Password

# Đặt giá trị này theo số phiên bản của SQL Server Management Studio bạn đang sử dụng.
# Ví dụ: nếu bạn đang sử dụng SSMS v18.12.1 → ghi là 18
# Nếu bạn sử dụng SSMS vv21.1.3 → ghi là 21
SSMS_VERSION=18

# JWT Secret
JWT_SECRET=mysecretkey123
